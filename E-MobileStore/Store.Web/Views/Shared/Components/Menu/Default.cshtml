@* Views/Shared/_Header.cshtml *@
@model IEnumerable<Store.WebService.ViewModels.CategoryVM>;
<ul class="menu" style="display: flex;flex-wrap: wrap;">
	@foreach (var menucate in Model)
	{
		if (menucate.Position == "home")
		{
			<li class="menu-ip " style="flex:auto">
				<a href="@Url.Action("Index", "Category", new { categoryUrl = menucate.CategoryUrl })">
					<span>@menucate.Name</span>
				</a>
			</li>
		}
	}
</ul>
